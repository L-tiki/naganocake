<div class="container">
  <div class="row mt-4">
    <h2>ログイン</h2>
  </div>

  <div class="row mt-3 ml-3">
    <h4>会員の方はこちらからログイン</h4>
  </div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="row mt-2 ml-5">
      <div class="col-2"><%= f.label "メールアドレス" %></div>
      <div class="col-5"><%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-100" %></div>
    </div>
    <div class="row mt-2 ml-5">
      <div class="col-2"><%= f.label "パスワード" %></div>
      <div class="col-5"><%= f.password_field :password, autocomplete: "current-password", class: "w-100" %></div>
    </div>
    <div class="row mt-2 ml-5">
      <div class="col-2"></div>
      <div class="col-3"><%= f.submit "ログイン", class: "btn btn-primary btn-block" %></div>
    </div>
  <% end %>

  <div class="row mt-3 ml-3">
    <h4>会員登録がお済みでない方</h4>
  </div>
  <div class="row mt-2 ml-5"><u><%= link_to "こちら", new_customer_registration_path %></u>　から新規登録を行ってください。</div>

</div>